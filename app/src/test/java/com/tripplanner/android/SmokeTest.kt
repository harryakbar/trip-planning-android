package com.tripplanner.android

import org.junit.Assert.assertTrue
import org.junit.Test

/** Placeholder unit test so the CI test step has something to run. */
class SmokeTest {
    @Test
    fun sanity() {
        assertTrue(true)
    }
}
