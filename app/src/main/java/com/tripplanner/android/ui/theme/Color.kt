package com.tripplanner.android.ui.theme

import androidx.compose.ui.graphics.Color

// Brand tokens ported from the web app (apps/tripplanningapp/src/styles/theme.css).
val Ink = Color(0xFF030213)          // --primary
val OnInk = Color(0xFFFFFFFF)        // --primary-foreground
val Surface = Color(0xFFFFFFFF)      // --background / --card (light)
val MutedLight = Color(0xFFECECF0)   // --muted
val MutedFg = Color(0xFF717182)      // --muted-foreground
val Destructive = Color(0xFFD4183D)  // --destructive

// Dark scheme approximations of the web `.dark` tokens.
val DarkBackground = Color(0xFF252525)
val DarkSurface = Color(0xFF252525)
val DarkOnSurface = Color(0xFFFAFAFA)
val DarkMuted = Color(0xFF444444)
